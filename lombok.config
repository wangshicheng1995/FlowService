# Lombok配置文件
# 禁用某些可能导致兼容性问题的功能

# 确保 Lombok 在 Java 17 下正常工作
lombok.addLombokGeneratedAnnotation = true

# 配置日志框架为 SLF4J
lombok.log.fieldName = log
lombok.log.fieldIsStatic = true

# 禁用可能引起问题的警告
lombok.anyConstructor.addConstructorProperties = false

# 确保兼容性
config.stopBubbling = true
